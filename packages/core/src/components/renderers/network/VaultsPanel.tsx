// VaultsPanel — the multi-vault connect/switch surface (Network → Vaults).
// Built out in the multi-vault phase. This stub keeps the shell compiling.
export function VaultsPanel() {
  return (
    <div style={{ color: "var(--text-muted)", fontSize: 13 }}>
      Connect and switch between multiple vaults — coming in the multi-vault phase.
    </div>
  );
}
