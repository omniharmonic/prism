// PublishPanel — the Publishing management surface (Network → Publish).
// Built out by the publishing-UX lane. This stub keeps the shell compiling.
export function PublishPanel() {
  return <div style={{ color: "var(--text-muted)", fontSize: 13 }}>Publishing…</div>;
}
