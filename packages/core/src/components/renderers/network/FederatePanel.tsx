// FederatePanel — the Federation management surface (Network → Federate).
// Built out by the federation-UX lane. This stub keeps the shell compiling.
export function FederatePanel() {
  return <div style={{ color: "var(--text-muted)", fontSize: 13 }}>Federation…</div>;
}
